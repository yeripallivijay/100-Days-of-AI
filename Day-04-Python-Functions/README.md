# Day 4 – Python Functional Programming

## Covered
- **Functions:** Definition, parameters (`*args`, `**kwargs`), and return values.
- **Lambda:** Anonymous functions for quick operations.
- **Map & Filter:** Functional tools to process iterables without explicit loops.

## Why it matters
- Essential for data preprocessing (e.g., using `map` to normalize data).
- Lambda functions are heavily used in Pandas for feature engineering.
