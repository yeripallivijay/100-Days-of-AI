# Day 5 – Python Modules & Packages

## Covered
- **Modularity:** Creating custom modules and packages with `__init__.py`.
- **Imports:** Absolute vs Relative imports (solving `ModuleNotFoundError`).
- **Standard Library:**
  - `math`: `sqrt`, `pi`, `ceil`, `floor`.
  - `random`: `randint`, `choice` (essential for sampling).
  - `datetime`: Handling time series data formats.

## Why it matters
- Production AI systems are built as packages, not notebooks.
- Understanding `sys.path` and imports is crucial for deploying models (e.g., with Flask/FastAPI).
